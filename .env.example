PORT=3000
NODE_ENV=development

DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=securepassword123
API_KEY=your_api_key_here

# Base URL and Path Configuration
# BASE_URL: Full URL where the API is accessible (used in Swagger documentation)
# BASE_PATH: Path prefix for all routes (e.g., /whatsapp/app)
# WebSocket connections will use BASE_PATH/socket.io as the endpoint
BASE_URL=http://localhost:3000
BASE_PATH=